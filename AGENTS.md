# AGENTS.md

## 0. 作用说明
本文件用于给 Codex 提供**持久化、项目级、可执行**的开发约束。
目标是：让 Codex 在这个仓库里优先做对的事、少做无关重构、保持本地优先、能跑能验。

---

## 1. 项目定位

### 1.1 项目名称
尚有新生（ShangYouXinSheng）

### 1.2 项目性质
AI 驱动的高端闲置品绿色流转平台，本地开发版。

### 1.3 架构定位
这是一个**本地优先、单体优先、原型/小团队优先**的仓库，不是生产级微服务底座。

### 1.4 核心技术栈
- Backend: Java 17+, Spring Boot 3, Spring Data JPA, Spring Security, JWT, Flyway, Spring Cache, Caffeine
- Frontend: Vue 3, TypeScript, Vite, Pinia, Vue Router, Element Plus, Axios, ECharts
- Database: MySQL 8
- Optional AI: Ollama (`llava`)
- Storage: local disk (`./uploads`)
- Docs: Swagger / SpringDoc
- Runtime: 100% local-first

---

## 2. 最高优先级原则（Hard Rules）

### 2.1 必须保持本地优先
默认实现必须优先满足：
- 一台机器即可跑通
- 尽量少依赖外部云服务
- 开发者克隆后可快速启动
- 可在未接入真实第三方服务时完整演示主链路

### 2.2 必须保持单体架构
不要主动把系统拆成微服务，不要引入网关、注册中心、配置中心。

### 2.3 不要默认引入以下内容
除非用户明确要求，否则不要引入或替换为：
- Redis
- Elasticsearch
- RocketMQ / Kafka
- 微服务 / Spring Cloud / Nacos / Gateway
- 云 OSS / S3
- 真实短信服务
- 真实支付 SDK
- OAuth 第三方登录
- 区块链 SDK
- Kubernetes / Helm / CI/CD 平台方案

### 2.4 优先做小而正确的修改
除非用户明确要求“重构”“重写”“升级架构”，否则：
- 优先局部修改
- 优先兼容既有结构
- 优先保留原模块边界
- 不要为了“看起来更优雅”而大面积改名/搬文件/换框架

### 2.5 不得绕过既有工程约束
- 数据库变更必须走 Flyway
- API 返回必须遵守统一响应体
- 安全校验不能被方便性绕开
- 前后端字段要对齐
- 不得用“临时 mock”污染正式模块边界，除非该模块本身就是本地 mock 设计

---

## 3. Codex 工作方式

### 3.1 接到任务后默认流程
Codex 在开始改代码前，默认遵循以下顺序：

1. 阅读本 AGENTS.md
2. 只定位与任务直接相关的模块/文件
3. 先理解现有实现，再改
4. 设计最小改动方案
5. 必要时同步修改后端、前端、类型、迁移脚本
6. 优先跑最小必要验证
7. 输出修改说明、影响文件、验证方式、风险点

### 3.2 大任务处理规则
若任务同时满足任一条件：
- 涉及 3 个以上模块
- 涉及数据库 + 后端 + 前端联动
- 涉及支付/认证/订单状态流转
- 涉及较大重构

则先给出一个**简短执行计划**，再实施改动。
执行计划应简洁，聚焦：
- 改哪些层
- 为什么这样改
- 先后顺序
- 风险点

### 3.3 默认输出风格
Codex 完成任务后，应尽量给出：
- 改了什么
- 改了哪些文件
- 是否新增迁移脚本
- 如何启动/验证
- 有哪些假设或限制

不要输出大段空泛解释，不要复述用户原话。

---

## 4. 仓库结构约定

### 4.1 预期根目录
```text
/
├── backend/
├── frontend/
├── docker-compose.yml
├── .env.example
├── README.md
└── AGENTS.md
```

### 4.2 后端包结构
```text
backend/src/main/java/com/syxs/
├── config/
├── common/
│   ├── result/
│   ├── exception/
│   ├── enums/
│   └── utils/
├── module/
│   ├── user/
│   ├── goods/
│   ├── ai/
│   ├── order/
│   ├── appraise/
│   ├── carbon/
│   └── file/
```

### 4.3 前端结构
```text
frontend/src/
├── api/
├── router/
├── stores/
├── components/
├── views/
├── styles/
└── types/
```

### 4.4 修改结构时的要求
- 优先沿用现有目录
- 不要新建“万能 util / helper / manager / facade”来逃避模块设计
- 共享能力放 `common/` 或明确的公共目录
- 页面逻辑放 `views/`
- 可复用 UI 放 `components/`
- API 请求统一放 `src/api/`
- 类型定义放 `src/types/`

---

## 5. 后端开发规则

### 5.1 基本原则
- 使用 Spring Boot 3 风格
- 使用 Spring Data JPA
- Controller 保持薄
- Service 负责业务
- Repository 负责持久化
- DTO / VO 作为接口边界
- 不要把 Entity 直接当作所有接口出参

### 5.2 注入与组织
- 优先构造器注入
- 避免字段注入
- 不要让 Controller 直接操作 Repository
- 不要把业务逻辑堆在 `if-else` 巨型方法里

### 5.3 接口规范
默认接口前缀：
```text
/api/v1
```

统一响应体：
```json
{
  "code": 0,
  "message": "成功",
  "data": {},
  "timestamp": 1710000000000
}
```

### 5.4 参数校验
- 优先使用 Bean Validation 注解
- 进入 Service 前尽量完成基础参数校验
- 业务规则错误使用统一业务异常
- 不要吞异常
- 不要返回随意拼装的错误 JSON

### 5.5 异常处理
- 统一走全局异常处理器
- 保持错误语义清晰
- 日志记录技术原因，响应暴露业务可理解信息
- 不在多个层级重复 try-catch 同一异常，除非需要补充上下文

### 5.6 JPA 规则
- 优先沿用已有 Repository 风格
- 简单查询优先使用方法名派生/Specification/JPQL
- 不要为了单个查询把整个模块切换为 MyBatis
- 注意懒加载、N+1 和循环引用风险
- 列表接口不要无节制返回深层关联对象

### 5.7 DTO / VO / Mapper
- 请求参数用 DTO
- 返回结果用 VO / Response DTO
- 若项目已存在 MapStruct，则优先复用
- 若没有统一 mapper 方案，不要为了一个小字段硬加复杂映射层

### 5.8 事务
- 涉及订单、库存/商品状态、积分结算等写操作时，明确考虑事务边界
- 事务方法应聚焦单个业务动作
- 不要把外部 HTTP 调用塞进长事务里

### 5.9 日志
- 记录关键状态变化、异常、外部调用失败
- 不记录密码、验证码明文、完整 token、敏感证件信息
- Debug 日志不要泛滥

---

## 6. 数据库与 Flyway 规则

### 6.1 强制规则
所有表结构、索引、种子数据变更，必须新增 Flyway 脚本，不得靠手工改表。

### 6.2 迁移命名
迁移文件命名格式：
```text
V{n}__description.sql
```

示例：
```text
V5__add_goods_favorite_count.sql
```

### 6.3 迁移编写要求
- 新增迁移，不修改已发布的旧迁移
- SQL 要可重复在干净环境执行
- 尽量写清楚注释
- 索引、默认值、非空约束要考虑已有数据兼容性
- 大字段/枚举变更要同步更新 Entity / DTO / 前端类型

### 6.4 本项目数据设计约束
重点表改动要特别谨慎：
- `t_user`
- `t_goods`
- `t_goods_image`
- `t_ai_estimate`
- `t_order`
- `t_appraise_order`
- `t_carbon_account`
- `t_carbon_record`

### 6.5 禁止行为
- 禁止在回答里建议“直接手动改数据库试试”
- 禁止通过 `ddl-auto=create/update` 替代 Flyway
- 禁止只改后端实体不改迁移

---

## 7. 安全与认证规则

### 7.1 认证方式
本项目采用 JWT 认证。

### 7.2 必须遵守
- 公开接口需要明确放行
- 受保护接口必须校验当前用户身份
- 涉及“我的商品 / 我的订单 / 我的碳账户”的接口必须校验归属
- 不要因为调试方便而临时去掉认证链并忘记恢复

### 7.3 本地模式特例
本地开发允许：
- 控制台打印 OTP
- 本地自动通过部分流程
- mock 支付

但这些特例必须：
- 有明确配置开关或模块边界
- 不要污染成通用生产逻辑
- 不要把 mock 行为写成默认不可区分的实现

### 7.4 敏感信息
- 不要硬编码真实密钥
- 不要在日志打印 token、refresh token、密码 hash 之外的明文密码
- 不要把测试账号逻辑散落在业务代码里

---

## 8. 文件存储规则

### 8.1 当前实现
默认使用本地文件存储：
```text
./uploads
```

### 8.2 修改要求
- 上传后返回相对路径或约定路径
- 通过 `/files/**` 访问静态资源
- 尽量保持 Controller 层不关心底层存储实现
- 若扩展云存储，优先通过接口抽象扩展，而不是重写全部调用方

### 8.3 禁止行为
- 不要默认接入 OSS / S3
- 不要写死开发机绝对路径
- 不要把上传文件塞进仓库源码目录

---

## 9. 缓存规则

### 9.1 当前缓存方案
- Spring Cache
- Caffeine

### 9.2 修改要求
- 小范围热点数据缓存优先复用现有缓存方案
- 关注缓存穿透、脏数据、过期时间
- 需要缓存时先评估是否真的必要

### 9.3 禁止行为
- 不要默认引入 Redis
- 不要把缓存当成修复查询逻辑的第一手段

---

## 10. AI 估价规则

### 10.1 能力边界
AI 估价是增强能力，不是系统唯一依赖。

### 10.2 必须保证
- Ollama 可用时走 Ollama
- Ollama 不可用时可降级到规则引擎
- 不得让“未安装 Ollama”导致核心链路不可用

### 10.3 改动要求
- 保持接口结果可追踪
- 记录 provider（如 `ollama` / `rule`）
- 注意估价结果解析失败时的兜底行为
- 不要把模型输出直接无校验落库

### 10.4 Prompt / 响应处理
- 若改动 AI 调用逻辑，优先保证输出结构稳定
- 对 JSON 解析失败、字段缺失、超时做容错
- 置信度、价格区间要有明确处理规则

---

## 11. 订单、支付、状态流转规则

### 11.1 订单模块属于高风险模块
改动订单、支付、收货、退款时，必须优先考虑状态流转完整性。

### 11.2 关键链路
典型链路：
1. 创建订单
2. 商品锁定
3. 模拟支付成功
4. 卖家发货
5. 买家确认收货
6. 触发结算
7. 触发碳积分入账

### 11.3 改动要求
- 状态流转必须单向可解释
- 避免重复支付、重复确认收货、重复结算
- 买家/卖家权限必须正确
- 退款逻辑要说明是本地 mock 还是真实规则

### 11.4 禁止行为
- 不要在多个地方散写状态码魔法值
- 不要跳过商品状态同步
- 不要让确认收货重复发积分

---

## 12. 碳账户规则

### 12.1 这是核心业务能力
碳账户、碳减排、积分流水不是装饰功能，不能随意弱化。

### 12.2 改动要求
- 保证汇总与流水一致
- 确认收货触发积分入账
- 证书生成保持可本地运行
- 与订单金额的计算公式保持一致

### 12.3 禁止行为
- 不要只更新总表不记流水
- 不要在前端硬编码积分值代替后端计算

---

## 13. 前端开发规则

### 13.1 基本原则
- Vue 3 Composition API
- TypeScript 优先
- Element Plus 优先
- Axios 统一请求层
- Pinia 只用于共享状态
- 页面逻辑尽量留在 `views`
- 可复用组件抽到 `components`

### 13.2 API 调用规则
- 所有请求统一走 `src/api/`
- 不要在页面里到处手写裸 `axios` / `fetch`
- token 注入、错误处理尽量复用拦截器
- 当前后端字段变化时，同步更新：
  - `api/`
  - `types/`
  - 使用该字段的视图/组件/store

### 13.3 TypeScript 规则
- 不要滥用 `any`
- 有明确结构的响应必须建类型
- 前后端枚举/状态码尽量对应
- 表单模型、接口响应、列表项类型分开定义更清晰时要分开

### 13.4 组件设计
- 复用性组件放 `components`
- 页面拼装逻辑放 `views`
- 不要把一个页面拆成过度碎片化的十几个文件，除非复用明显
- 管理后台可 PC 优先，不强求移动端

### 13.5 状态管理
只有以下情况优先使用 Pinia：
- 登录态
- 用户信息
- 购物车/收藏等跨页共享状态
- 碳账户摘要等多页共享信息

局部 UI 状态优先留在组件内部。

### 13.6 样式规范
- 优先复用全局变量和既有样式
- 保持绿色、清爽、偏高端的产品调性
- 中文文案自然、简洁、产品化
- 避免随手写大量内联样式

### 13.7 双端适配
本项目默认一套 Vue 代码适配 PC / 移动端。
改 UI 时：
- 保持基础响应式能力
- 商品、订单、个人中心等核心页面要兼顾移动端
- 后台管理页面可默认 PC 优先
- 不要无故拆成双前端项目

---

## 14. 命名与代码风格

### 14.1 Java 命名
- Controller: `XxxController`
- Service: `XxxService`
- Repository: `XxxRepository`
- Entity: 领域名词
- DTO: `XxxCreateDTO` / `XxxUpdateDTO` / `XxxQueryDTO`
- VO/Response: `XxxVO` / `XxxResponse`

### 14.2 Vue / TS 命名
- 页面：`XxxView.vue` 或业务清晰的页面名
- 组件：`XxxCard.vue` / `XxxPanel.vue` / `XxxDialog.vue`
- store：`xxx.store.ts`
- API：按模块拆分，如 `goods.ts`, `order.ts`

### 14.3 风格要求
- 优先可读性
- 方法短小、单一职责
- 删除死代码、重复代码、无用 import
- 不要炫技式抽象
- 非显而易见逻辑可加少量注释
- 注释解释“为什么”，不要解释“这行代码在干嘛”

---

## 15. 删除无用代码 / 重构规则

### 15.1 可以做的优化
在不改变公开行为的前提下，可以安全做这些事：
- 删除未使用 import
- 删除确定无调用的私有方法/常量
- 合并重复逻辑
- 提取过长方法中的局部私有函数
- 修复明显命名歧义
- 去掉无意义日志/注释

### 15.2 需要谨慎的优化
以下操作只有在确认影响面后再做：
- 改公共方法签名
- 改接口出参字段
- 调整实体字段名
- 移动类文件位置
- 改路由路径
- 改数据库字段

### 15.3 禁止的“伪优化”
- 只为“代码更优雅”重写整模块
- 把容易理解的业务代码抽成过度抽象的模板体系
- 把当前可运行代码换成大型新框架
- 混入与当前任务无关的大量格式化/重命名提交

---

## 16. 与 PRD 的关系

### 16.1 以代码为准，兼顾 PRD 意图
当 PRD 与现有代码不完全一致时，优先级如下：
1. 当前仓库真实代码结构
2. 本地优先的产品约束
3. PRD 的业务目标
4. 对未来生产化的可扩展留口

### 16.2 不要把 AGENTS 当成 PRD 复读机
实现任务时，关注：
- 结构约束
- 修改边界
- 验证要求
- 风险点

不要为了“贴合文档”而把所有需求描述原样抄进代码或输出。

---

## 17. 运行与验证

### 17.1 本地启动
MySQL:
```bash
docker compose up -d
```

Backend:
```bash
cd backend
./mvnw spring-boot:run
```

Frontend:
```bash
cd frontend
npm install
npm run dev
```

### 17.2 常用访问地址
- Frontend: `http://localhost:5173`
- Backend: `http://localhost:8080`
- Swagger: `http://localhost:8080/swagger-ui.html`

### 17.3 验证策略
Codex 改代码后，优先做与改动规模匹配的验证。

#### 后端最小验证
优先尝试仓库已有命令，常见包括：
```bash
cd backend
./mvnw test
```

若没有完善测试但需要快速验证编译：
```bash
cd backend
./mvnw -q -DskipTests compile
```

#### 前端最小验证
优先尝试仓库已有脚本，常见包括：
```bash
cd frontend
npm run build
```

若仓库存在类型检查脚本，也应运行：
```bash
cd frontend
npm run type-check
```

### 17.4 验证要求
- 没跑测试就不要假装跑了
- 跑不了就明确说明原因
- 至少说明建议如何人工验证
- 改接口时尽量说明一个最短联调路径

---

## 18. 提交前自检清单（Codex 必查）

### 18.1 后端
- 是否改动了正确模块，而不是绕路实现
- Controller / Service / Repository / DTO 是否对齐
- 是否破坏统一响应体
- 是否考虑认证与归属校验
- 是否需要 Flyway 迁移
- 是否存在明显空指针/事务/重复结算风险
- 是否引入了不必要新依赖

### 18.2 前端
- API 层是否同步更新
- TypeScript 类型是否同步更新
- 页面是否还能正常加载
- 关键按钮文案是否自然
- 是否考虑了移动端基础展示
- 是否避免把大量业务逻辑直接堆到模板中

### 18.3 联动功能
- 前后端字段名是否一致
- 状态码/状态值是否一致
- 上传路径/静态访问路径是否一致
- 列表与详情是否使用同一套关键字段定义

---

## 19. Codex 输出模板（建议）

完成任务时，尽量按下面结构组织回答：

```text
1. 修改内容
2. 影响文件
3. 数据库变更（如有）
4. 验证方式
5. 注意事项 / 假设
```

示例：
```text
修改内容：
- 新增商品收藏接口
- 商品详情返回收藏状态
- 前端详情页增加收藏按钮

影响文件：
- backend/.../GoodsController.java
- backend/.../GoodsService.java
- backend/.../FavoriteRepository.java
- backend/src/main/resources/db/migration/V5__add_favorite_table.sql
- frontend/src/api/goods.ts
- frontend/src/views/goods/GoodsDetail.vue

数据库变更：
- 新增 t_favorite 表

验证方式：
- 启动前后端
- 登录后打开商品详情页，点击收藏
- 刷新后检查收藏状态是否保留

注意事项：
- 仅实现当前用户收藏，不含收藏列表页
```

---

## 20. 明确禁止事项（Must Avoid）

Codex 在此仓库中不得默认做以下事情：

- 不得擅自把 JPA 换成 MyBatis / MyBatis-Plus
- 不得擅自把单体拆成微服务
- 不得默认引入 Redis / ES / MQ / OSS / OAuth / 真实支付
- 不得绕过 Flyway 直接改表
- 不得随意改 API 响应结构
- 不得把本地 mock 行为伪装成生产逻辑
- 不得大面积无关重命名
- 不得把用户当前任务扩大成“顺手重构全项目”
- 不得生成与现有项目风格严重不符的代码
- 不得假设不存在的 npm script / Maven profile 一定存在
- 不得在未验证时声称“已完全可运行”

---

## 21. 任务优先级判断

当多个目标冲突时，按以下优先级决策：

1. 正确性
2. 安全性
3. 与现有结构兼容
4. 本地优先可运行
5. 可维护性
6. 性能优化
7. 未来生产扩展性
8. 代码“优雅度”

---

## 22. 最终原则

这个项目的核心不是“堆最先进的架构”，而是：

- 让本地开发足够顺滑
- 让主业务链路真实可演示
- 让代码结构清晰稳定
- 让 Codex 每次改动都小、准、稳
- 为未来升级预留接口，但不提前过度建设

当不确定时：
**优先选择更简单、更本地化、更符合现有结构的实现。**
